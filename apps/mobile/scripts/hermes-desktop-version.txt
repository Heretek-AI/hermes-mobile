# Pinned commit SHA for fathah/hermes-desktop.
#
# vendor-renderer.sh rsyncs the desktop's React renderer from
# src/renderer/src + src/shared/ into packages/renderer/ and
# packages/shared/. The CI workflow clones this repo at the SHA
# below into /tmp/hermes-desktop and points the vendor script at it
# via HERMES_DESKTOP_SRC / HERMES_DESKTOP_SHARED_SRC env vars.
#
# Local devs can keep using review/hermes-desktop/ as the source
# (the script falls back to that when the env vars are unset).
#
# Bump this file when the upstream release cadence warrants it; the
# patch files in apps/mobile/scripts/renderer-patches/ are PR'd
# upstream and removed once they land.
3cbf7800bf0a773d3c7eb361c63eb49b60ca2926
