package com.nousresearch.hermes

/**
 * HermesAPIPlugin — **stub**. The native-Compose-only
 * architecture (per the parity plan) removed the WebView+
 * Capacitor path. This file is retained so the historical
 * `android-runner/app/src/main/kotlin/com/nousresearch/hermes/`
 * tree stays symmetric; a future WebView re-introduction
 * would restore the original 700-line Capacitor bridge from
 * git history (see plan §Phase 0d).
 *
 * v1: the class is a no-op; nothing references it.
 */
@Suppress("unused")
class HermesAPIPlugin
