{
  "name": "@hermes-mobile/app",
  "version": "0.1.0",
  "private": true,
  "description": "Hermes Agent Android app. The Android project is at apps/mobile/android/; the Kotlin sources are canonicalised from android-runner/ via apps/mobile/scripts/setup-android.sh.",
  "scripts": {
    "android:assemble:debug": "cd android && ./gradlew assembleDebug",
    "android:install:debug": "cd android && ./gradlew installDebug",
    "android:assemble:release": "cd android && ./gradlew assembleRelease",
    "android:drift:check": "bash ../../script/check-kotlin-drift.sh",
    "android:sync": "bash scripts/setup-android.sh"
  }
}
